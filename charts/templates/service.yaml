apiVersion: v1
kind: Service
metadata:
  name: {{ include "provablemarkets-demo.fullname" . }}
  labels:
    {{- include "provablemarkets-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provablemarkets-demo.selectorLabels" . | nindent 4 }}
